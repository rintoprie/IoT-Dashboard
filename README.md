# IoT-Dashboard
Simple dashboard for interaction with IoT devices via MQTT.  
The code is so simple that it uses plain html and plain css or bootstrap for easy learning.  
Connection to MQTT use plain javascript with simple mqtt library.  
